---
import Layout from "../../layouts/Layout.astro";
import DashboardApp from "../../components/dashboard/DashboardApp";
import { SITE } from "../../config";
---

<Layout title={`Live — ${SITE.name}`}>
  <DashboardApp client:load />
</Layout>
